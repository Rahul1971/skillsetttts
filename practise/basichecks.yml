- name: "Install Nginix"
  hosts: localhost
  tasks:
    - name: "Install Nginix"
      shell: "dnf module install nginx:1.24 -y"

    - name: "update yum"
      dnf:
        name: "*"
        state: latest


      #dnf:
       # name: nginix
       # state: installed
        #when: x.stdout != 0

    #- name: "Start Nginix service"
    #  service:
    #    name: nginix
    #    state: started
