- name: "Install tomcat"
  hosts: all
  #connection: local
  tasks:

    - name: "Install tomcat"
      dnf:
       name: "tomcat"
       state: installed

    - name: "Start tomcat service"
      service:
        name: tomcat
        state: started



       #"dnf module install tomcat:1.24 -y"

   # - name: "update yum"
   #   dnf:
   #     name: "*"
   #     state: latest


      #dnf:
       # name: nginix
       # state: installed
        #when: x.stdout != 0

    #- name: "Start Nginix service"
    #  service:
    #    name: nginix
    #    state: started
